# LAYOUT_SHELL_LAW — единна обвивка (хедър + долна навигация)

**Версия:** v1.0
**Дата:** 2026-05-10
**Статус:** АКТИВЕН ЗАКОН — задължителен за всички основни страници
**Промотва се в:** DESIGN_SYSTEM v4.3 след преглед

---

## 0. Защо този закон съществува

Една и съща обвивка на всяка основна страница е причината потребителят
никога да не „се изгуби" в приложението. До S136 имахме две паралелни
системи от партиали (`partials/*.php` срещу `design-kit/partial-*.html`)
с леко различен хедър и долна навигация. Всяка страница включваше едното
ИЛИ другото — резултатът беше визуална мозайка, която направи P11
chat.php rewrite да удари AUTO-ROLLBACK на iter 5 (DOM diff 31% вместо
очакваните <5%, защото header/nav не съвпадаха с макета).

Главният урок е: **ЕДНА обвивка, ЕДНО източник на истина**.

P11 (последният одобрен макет — `mockups/P11_detailed_mode.html`) е
ЕТАЛОН за разширен режим. P10 (`mockups/P10_lesny_mode.html`) е ЕТАЛОН
за лесен режим. Този документ описва единните правила, които всички
основни страници (изброени по-долу) спазват 1:1.

---

## 1. ЗАКОН ЗА ХЕДЪР (горна лента — 2 реда)

### 1.1 Ред 1 — `rms-header` (7 елемента в точен ред)

```
[ RUNMYSTORE.AI ] [ ПЛАН ] [ spacer ──────── ] [ 🖨 ] [ ⚙ ] [ ⤴ ] [ ☀/🌙 ]
       1            2          3                  4     5    6     7
```

| # | Елемент              | Поведение                                                        |
|---|----------------------|------------------------------------------------------------------|
| 1 | `RUNMYSTORE.AI`      | brand wordmark, gradient shimmer, линква към началото            |
| 2 | `.rms-plan-badge`    | реалният план на tenant-а: **PRO / START / FREE / BUSINESS**     |
| 3 | `.rms-header-spacer` | flex: 1 — избутва дясната група                                  |
| 4 | Принтер              | bluetooth printer toggle (`rmsOpenPrinter()`)                    |
| 5 | Настройки            | линк към `settings.php`                                          |
| 6 | Изход                | dropdown с „Изход →" линк към `logout.php`                       |
| 7 | Тема                 | sun/moon toggle (`rmsToggleTheme()`)                             |
|   |                      |                                                                  |

**Правила:**
- Редът на 7-те елемента е свещен (не разменяй, не премахвай, не добавяй).
- Височина 56px, sticky top, z-index 50.
- Plan badge показва реалния план — никога hardcoded „PRO".
- Никога не дублирай тези елементи на body нивото — те живеят само в
  `partials/header.php`.

### 1.2 Ред 2 — `rms-subbar` (3 елемента в точен ред)

```
[ 🏬 ENI ˅ ]   [ НАЧАЛО ]   ───────   [ ← Лесен ]   [ Разширен → ]
   shop chip    where-we-are            mode toggle    mode toggle
   (1)          (2)                     (3a, в разш.)  (3b, в лесен)
```

| # | Елемент            | Поведение                                                      |
|---|--------------------|----------------------------------------------------------------|
| 1 | Магазин чип        | dropdown с активния магазин (напр. „ENI"); смяна → switcher    |
| 2 | Where-we-are       | моноширинен label на текущия модул („НАЧАЛО", „СКЛАД", …)      |
| 3 | Mode toggle        | връзка към другия режим (виж 1.3 за етикета)                    |

**Правила:**
- Subbar присъства на ВСЯКА основна страница (и в двата режима).
- Магазин чипът е винаги вляво. Where-we-are веднага след него.
- Mode toggle е винаги вдясно (последен елемент на реда).
- В лесен режим се добавя АМБЕР „Продажба" pill вдясно от mode toggle —
  виж §3.

### 1.3 Етикет на mode toggle (огледално правило)

| Текущ режим          | Бутон показва   | Линкът води към |
|----------------------|-----------------|-----------------|
| Разширен (detailed)  | `← Лесен`       | лесен режим     |
| Лесен (simple)       | `Разширен →`    | разширен режим  |

Етикетът показва **накъде водиш** потребителя, не **къде си**. Стрелката
сочи посоката на прехода (наляво за връщане към лесен, надясно за
напред към разширен).

> **Бележка за терминологията:** в по-стари макети („Подробен") и нов
> текст („Разширен") са синоними и сочат към един и същ режим — детайлния
> работен интерфейс. Канонът от този закон е **„Разширен"**.

---

## 2. ЗАКОН ЗА ДОЛНА НАВИГАЦИЯ (`rms-bottom-nav`)

### 2.1 Структура

Точно **4 таба** с анимирани топчета (orb стил от P11), в точен ред:

```
┌──────────────────────────────────────────────────────────────┐
│   AI         Склад        Статистики       Продажби          │
│  (orb)       (orb)         (orb)            (orb)            │
└──────────────────────────────────────────────────────────────┘
```

| # | Таб         | Активен за модули                                                   |
|---|-------------|---------------------------------------------------------------------|
| 1 | AI          | chat, simple, life-board, index                                     |
| 2 | Склад       | warehouse, inventory, transfers, deliveries, suppliers, products    |
| 3 | Статистики  | stats, finance                                                      |
| 4 | Продажби    | sale                                                                |

> **Implementation note:** действащият `partials/bottom-nav.php` използва
> етикетите „Справки" и „Продажба" (singular). Канонът от този закон е
> „Статистики" и „Продажби" (plural). Несъответствието е известно и
> се hармонизира при следващ docking sweep — кодът е Mutable, законът
> не е.

### 2.2 Видимост

| Режим              | Долна навигация |
|--------------------|-----------------|
| **Разширен**       | **ДА** — винаги видима                |
| **Лесен**          | **НЕ** — никога не се рендерира       |
| Вторични страници  | **НЕ** — виж §4                       |

В лесен режим долна навигация изобщо не съществува в DOM-а. Това е
същината на „лесен" — Пешо не вижда 4 таба, които да го объркат.

### 2.3 Активен таб (анимирано топче)

- Active tab има цветен orb (radial gradient в неговия hue) над иконата
- Под иконата — accent цветен бар (4px, conic-glow в dark)
- Останалите 3 таба са с `--text-muted` цвят, без бар, без orb

Орбит-анимацията (orbSpin keyframes — 5s linear infinite) е свещена и
върви само на активния таб.

---

## 3. ЗАКОН ЗА ЛЕСЕН РЕЖИМ

Лесен режим няма долна навигация — затова навигацията се пренася на
subbar реда, и се добавя един едър „Продажба" бутон.

### 3.1 Back-arrow в хедъра

Когато сме навлезли във ВТОРИЧНА страница ОТ life-board (напр. от
„Продажба"-карта в life-board към `sale.php`), вляво от магазин чипа
(тоест преди елемент №1 на subbar реда) се рендерира:

```
[ ← ]   [ 🏬 ENI ˅ ]   [ КЪДЕ СМЕ ]   ───   [ Разширен → ]   [ ПРОДАЖБА ]
 back     shop chip      where             mode toggle        amber pill
```

**Правила за back-arrow:**
- Показва се САМО в лесен режим И когато текущият модул НЕ е
  `life-board / simple / index`.
- Натискане → връща на `life-board.php` (а не `history.back()` —
  history е ненадежден от deep links).
- На самия life-board стрелката НЕ се рендерира (защото няма откъде
  да си дошъл).

### 3.2 „Продажба" pill (амбер) — десен край на subbar

Защото долна навигация липсва, Пешо трябва да стигне до продажба с
**едно натискане** от всяка лесна страница. Решението е едър pill
бутон на subbar реда, дясно от mode toggle:

```
─── [ Разширен → ]   [ ПРОДАЖБА ]
                       ↑
                       amber/оранжев, pill форма, голям
```

**Правила за „Продажба" pill:**
- Появява се САМО в лесен режим.
- Цвят: амбер/оранжев (`hsl(38 80% 50%)` ± dark-mode вариант).
- Форма: pill (`var(--radius-pill)`), по-висок от mode toggle (44px).
- Текст: „Продажба" (главна буква, font-weight 800).
- Натискане → `sale.php`.
- Винаги последен елемент на subbar реда.
- На самия `sale.php` (когато сме там) бутонът все пак се показва —
  работи като визуален anchor, не се скрива.

### 3.3 Шапка на лесен режим — обобщение

```
┌─────────────────────────────────────────────────────────────┐
│  RUNMYSTORE.AI  [PRO]  ·····  🖨  ⚙  ⤴  ☀                 │  rms-header
├─────────────────────────────────────────────────────────────┤
│  [←]  🏬 ENI ˅   НАЧАЛО   ····   [Разширен →]  [ПРОДАЖБА]  │  rms-subbar
└─────────────────────────────────────────────────────────────┘
        ↑                                          ↑
    back-arrow                                amber pill
    (само ако                                 (само в
     не сме на                                 лесен режим)
     life-board)
```

---

## 4. ЗАКОН ЗА ОСНОВНИ И ВТОРИЧНИ СТРАНИЦИ

### 4.1 Основни страници (получават пълната обвивка)

Тези страници включват И хедър, И subbar, И долна навигация (последната
само в разширен режим):

| Файл                | Режим по подразб. | Долна навигация (в разш.) |
|---------------------|-------------------|---------------------------|
| `life-board.php`    | Лесен             | няма                      |
| `chat.php`          | Разширен          | AI таб активен            |
| `products.php`      | Разширен          | Склад таб активен         |
| `sale.php`          | Разширен / Лесен  | Продажби таб активен      |
| `warehouse.php`     | Разширен          | Склад таб активен         |
| `stats.php`         | Разширен          | Статистики таб активен    |
| `deliveries.php`    | Разширен          | Склад таб активен         |
| `transfers.php`     | Разширен          | Склад таб активен         |
| `inventory.php`     | Разширен          | Склад таб активен         |

Всяка от тези страници МОЖЕ да бъде отворена и в лесен, и в разширен
режим. Превключването на `$_SESSION['mode']` е единственият контрол —
не се изисква отделен файл за лесна и разширена версия.

### 4.2 Вторични страници (само хедър, без долна навигация)

Тези страници:
- получават хедър ред 1 и ред 2 (subbar);
- НЕ получават долна навигация в нито един режим;
- back-arrow поведение е същото като при основни страници в лесен режим.

| Файл / клас                      | Защо без долна навигация              |
|----------------------------------|---------------------------------------|
| `ai-studio.php`                  | работен поток, не основна дестинация  |
| Всички модални / overlay екрани  | не са страници, са състояния          |
| Wizard стъпки                    | линеен поток, навигация нагоре чрез X |
| Print екран                      | preview / one-shot действие           |
| `printer-setup.php`              | конфигурация, не оперативна страница  |

**Правило:** ако една страница не е в списъка от §4.1, тя НЕ слага
долна навигация — независимо в какъв режим е tenant-ът.

---

## 5. ASCII схеми на двата режима

### 5.1 Разширен режим (detailed) — пълна обвивка

```
┌─────────────────────────────────────────────────────────────┐
│  RUNMYSTORE.AI  [PRO]  ·····  🖨  ⚙  ⤴  ☀                 │  rms-header
├─────────────────────────────────────────────────────────────┤
│  🏬 ENI ˅   ЧАТ С AI   ·····················  [← Лесен]    │  rms-subbar
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                                                             │
│                                                             │
│                    .                                        │
│                  .   .       BODY на страницата             │
│                .       .                                    │
│                  .   .       (chat / products /             │
│                    .          stats / sale / …)             │
│                                                             │
│                                                             │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│      AI       Склад      Статистики      Продажби           │  rms-bottom-nav
│     (orb)      .            .              .                │  (4 таба)
└─────────────────────────────────────────────────────────────┘
```

### 5.2 Лесен режим (simple) — без долна навигация

```
┌─────────────────────────────────────────────────────────────┐
│  RUNMYSTORE.AI  [PRO]  ·····  🖨  ⚙  ⤴  ☀                 │  rms-header
├─────────────────────────────────────────────────────────────┤
│  [←]  🏬 ENI ˅  СКЛАД  ····  [Разширен →]   [ПРОДАЖБА]     │  rms-subbar
│   ↑                                            ↑            │  (back-arrow
│   back-arrow                                amber           │   се крие
│   само ако                                  pill            │   на life-
│   не сме на                                 (винаги         │   board)
│   life-board                                в лесен)        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                                                             │
│                                                             │
│                  Life Board cards                           │
│                  (или body на                               │
│                  вторичната страница                        │
│                  в лесен режим)                             │
│                                                             │
│                                                             │
│                                                             │
│                                                             │
│                                                             │
│            (тук НЕ се рендерира долна навигация)            │
└─────────────────────────────────────────────────────────────┘
```

---

## 6. SACRED — не модифицирай

Долните елементи са свещени. Всяка промяна изисква актуализация на
ТОЗИ закон ПРЕДИ кода.

### 6.1 Header ред 1
- 7-те елемента и техният ред (brand → plan → spacer → 🖨 → ⚙ → ⤴ → ☀)
- Височина 56px
- Sticky top, z-index 50
- Plan badge показва РЕАЛНИЯ план (никога hardcoded)
- Theme toggle вика `rmsToggleTheme()` (никога legacy `toggleTheme()`)

### 6.2 Header ред 2 (subbar)
- Магазин чип винаги пръв (вляво)
- Mode toggle винаги последен (вдясно) в разширен режим
- В лесен режим: back-arrow преди магазин чипа (условно), „Продажба"
  pill след mode toggle (винаги)
- Етикетът на mode toggle сочи накъде водиш, не къде си

### 6.3 Bottom nav
- Точно 4 таба, не 3 не 5
- Ред: AI → Склад → Статистики → Продажби (никога разместен)
- Активен таб има orb + bar + accent color
- НЕ се рендерира в лесен режим
- НЕ се рендерира на вторични страници (§4.2)

### 6.4 Animations
- `orbSpin` 5s linear infinite — само на активния орб
- `conicSpin` 3s linear infinite — на plan-badge ring
- `rmsBrandShimmer` 4s linear infinite — на brand wordmark
- Всички животни се убиват от `prefers-reduced-motion: reduce`

### 6.5 Един източник на истина
- `partials/header.php` — единственият хедър партиал.
- `partials/bottom-nav.php` — единственият долна навигация партиал.
- `design-kit/partial-header.html` и `design-kit/partial-bottom-nav.html`
  са LEGACY и не се добавят в нови страници. Премахването им е на
  отделна задача (PARTIALS_STANDARD §Phase 4).

---

## 7. Контракт със сесията

| Ключ                  | Тип      | Стойности                  | Записва от           | Чете от                   |
|-----------------------|----------|----------------------------|----------------------|---------------------------|
| `$_SESSION['mode']`   | string   | `'simple'` или unset       | `life-board.php`     | `partials/header.php`     |

- Когато ключът е `'simple'` → лесен режим (skin = §3, §5.2).
- Когато ключът е unset → разширен режим (skin = §1, §2, §5.1).
- Изход (`logout.php`) изтрива целия `$_SESSION`, включително mode.
- Mode toggle бутонът в subbar експлицитно записва или изтрива ключа
  при клик.

Всички детайлни страници (chat, products, sale, settings, warehouse, …)
НЕ четат и НЕ пишат `$_SESSION['mode']`. Партиалът е единственият
консумент. Това опростява контракта: един писател (life-board + mode
toggle), един читател (header.php).

---

## 8. Връзка с останалите документи

- **`DESIGN_SYSTEM.md` §1.4 + §3** — низкониво на header/nav (CSS,
  токени). Този закон е НАД него: ако влязат в конфликт, печели
  LAYOUT_SHELL_LAW, и DESIGN_SYSTEM се синхронизира.
- **`PARTIALS_STANDARD.md`** — PHP имплементацията на партиалите.
  Този закон е спецификацията; PARTIALS_STANDARD е изпълнението.
- **`mockups/P11_detailed_mode.html`** — визуален еталон за разширен
  режим.
- **`mockups/P10_lesny_mode.html`** — визуален еталон за лесен режим.
- **`mockups/P2_home_v2.html` и `P3_list_v2.html`** — еталони за
  body-сектори на склад / списъци; обвивката им трябва да е същата
  както в P11.

---

END v1.0
